import { UploadFilesDirective } from './upload-files.directive';

describe('UploadFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFilesDirective();
    expect(directive).toBeTruthy();
  });
});
