import { EditUploadFilesDirective } from './edit-upload-files.directive';

describe('UploadFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new EditUploadFilesDirective();
    expect(directive).toBeTruthy();
  });
});
